box: wercker/default
build:
  steps:
    - arjen/hugo-build:
        version: 1.10.1
        theme: hugo-uno
        flags: --buildDrafts=false
deploy:
  steps:
    - lukevivier/gh-pages@0.2.1:
        token: $GITHUB_TOKEN
        basedir: public
