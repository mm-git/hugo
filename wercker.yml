box: debian 
build:
  steps:
    - arjen/hugo-build:
        theme: hugo-uno
        flags: --buildDrafts=false
deploy:
  steps:
    - install-packages:
        packages: git ssh-client
    - leipert/git-push:
        gh_oauth: $GIT_TOKEN
        repo: mm-git/mm-git.github.io
        branch: gh-pages
        basedir: public
